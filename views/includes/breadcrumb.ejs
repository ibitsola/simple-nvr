<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/">CCTV Viewer</a></li>
        <% route.forEach(folder => { %>
        <li class="breadcrumb-item"><a href="<%= folder.route %>"><%= folder.displayName || folder.name %></a></li>
        <% }); %>
    </ol>
</nav>